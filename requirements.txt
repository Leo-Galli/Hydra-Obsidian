# HYDRA OBSIDIAN v2.1.0 — Dependencies
# Install with: pip install -r requirements.txt

streamlit>=1.40.0      # Dashboard UI + session state
pyzmq>=26.0.0          # ZeroMQ ROUTER/DEALER messaging
psutil>=6.0.0          # CPU / RAM / Disk telemetry
plotly>=5.24.0         # Real-time time-series charts
pandas>=2.2.0          # Data structures (Streamlit internal use)
numpy>=1.26.0          # Numerical arrays (Plotly dependency)
